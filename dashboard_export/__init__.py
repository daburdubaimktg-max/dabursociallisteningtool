"""Excel dashboard export — the 'live' competitor social-growth workbook.

'Live' means: the workbook is regenerated from the current state of the follower
store on every call, so a fresh monthly collection shows up as a new column the next
time the dashboard is built. It mirrors the structure of the source Master Social
Tracker (per-platform brand × month matrices, a growth view, an IG-vs-TikTok combined
view, and category/region rollups) plus native charts.
"""
